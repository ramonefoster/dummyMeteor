Make sure you are on a working directory that has no Space " " in the path. Keras has a problem with it...

`python -m venv virtual_env`

Install the requirements

`pip install -r requirements`

run main.py :)
